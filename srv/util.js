module.exports.checkRequired = ({...params}) => {
  // params.forEach(param => {
  //   if (param === undefined || param === null || param === '') {
  //     return false;
  //   }
  // });
  return true;
};
